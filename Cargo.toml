[package]
name = "favicon-generator"
version = "0.1.0"
authors = ["Magic Len <len@magiclen.org>"]
repository = "https://github.com/magiclen/favicon-generator"
homepage = "https://magiclen.org/favicon-generator"
keywords = ["favicon"]
categories = ["command-line-utilities"]
description= "It helps you generate favicons for different platforms."
readme = "README.md"
license = "MIT"

[profile.release]
panic = "abort"

[dependencies]
clap = "2"
image-convert = "0.5"