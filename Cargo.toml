[package]
name = "favicon-generator"
version = "0.3.8"
authors = ["Magic Len <len@magiclen.org>"]
edition = "2018"
repository = "https://github.com/magiclen/favicon-generator"
homepage = "https://magiclen.org/favicon-generator"
keywords = ["favicon", "generating", "web-app", "icon"]
categories = ["command-line-utilities"]
description= "It helps you generate favicons with different formats and sizes."
readme = "README.md"
license = "MIT"

[badges.travis-ci]
repository = "magiclen/favicon-generator"
branch = "master"

[profile.release]
panic = "abort"

[dependencies]
clap = "2.33.1"
concat-with = "0.2"
terminal_size = "0.1"

execute = "0.2.4"
image-convert = "0.9.5"
scanner-rust = "2.0.8"
serde_json = "1"
tera = "1.0.0"
lazy_static = "1"
slash-formatter = "3"

[dependencies.validators]
version = "0.20"
features = ["serdely"]